<section class="card">
  <a href="/Quiz/{{id}}">
    <header>{{name}}</header>
    {{!-- switch this to quiz images --}}
    <img src="/images/{{paintings.[0].filename}}" alt="{{paintings.[0].description}}">
  </a>
</section>
